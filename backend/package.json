{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "esbuild function.ts --bundle --minify --format=esm --platform=node --target=es2020 --outfile=dist/index.mjs",
    "postbuild": "cd dist && zip -j index.zip index.mjs*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "esbuild": "^0.25.8",
    "typescript": "catalog:"
  }
}
